/// The scheme used for network requests.
public extension PSNetwork {
    /// The available schemes for network requests.
    enum Scheme: String {
        /// The HTTP scheme.
        case http
        /// The HTTPS scheme.
        case https
    }
}
